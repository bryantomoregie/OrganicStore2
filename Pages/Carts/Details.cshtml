@page
@model OrganicStore2.Pages.Carts.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Cart</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Cart.Client)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Cart.Client.Id)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Cart.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
